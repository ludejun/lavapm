package com.dmp.core.controller.admin;

import td.enterprise.dmp.base.web.*;
import org.springframework.stereotype.*;
import org.springframework.web.bind.annotation.*;
import org.apache.log4j.*;

@Controller
@RequestMapping({ "/admin" })
public class AzkabanRestController extends BaseController
{
    public static final Logger logger = Logger.getLogger(AzkabanRestController.class);
    
}
